package com.hernan.pe.util;

import com.hernan.pe.security.entity.Rol;
import com.hernan.pe.security.enums.RolNombre;
import com.hernan.pe.security.service.RolService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

//@Component
public class CreateRoles/* implements CommandLineRunner*/  {
/*
 * se ejecuta solo una vez para crear los roles
	@Autowired
	RolService rolService;

	@Override
	public void run(String... args) throws Exception {
		Rol rolAdmin = new Rol(RolNombre.ROLE_ADMIN);
		Rol rolUser = new Rol(RolNombre.ROLE_USER);
		rolService.save(rolAdmin);
		rolService.save(rolUser);

	}
*/
}
